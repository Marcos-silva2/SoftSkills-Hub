# Copie este arquivo para .env e preencha os valores antes de ir para produção.
# Nunca commite o arquivo .env real em repositórios públicos.

# Chave secreta para assinar os tokens JWT.
# Gere uma chave forte com: python -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=TROQUE-POR-UMA-CHAVE-FORTE-E-ALEATORIA
